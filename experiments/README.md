# Experiments

Each future run will have an immutable manifest linking source data, predictor, explanation, evidence packets, interventions, prompts, raw LLM responses, validation results, human annotations, and derived metrics by identifier and checksum.

Bulk outputs belong under the ignored `experiments/outputs/` path or external artifact storage. Small redacted manifests and summary tables may be committed when their inputs are shareable and their provenance is complete.
